# 비선형구조(순서가 불규칙하여 복잡한 구조)를 빠짐없이 검색하는 방법
# 그중 스택을 이용하는 방법 = DFS (Depth First Search)

# DFS 방법
# 1. 시작에서 한 경로로 깊이 탐색
# 2. 더이상 갈 곳이 없으면 마지막 갈림길로 돌아옴
# 3. 다른 방향으로 탐색하여 모든 지점 순회

# DFS 추상 알고리즘

'''
visited=[]; stack=[] #초기화

def DFS (v):
    v 방문
    visited[v] <- true
        do {
            if (v의 인접 정점중 방문 안한 w 찾기)
                push(v)
            while (w):
                w 방문
                visited[w] <- turtle
                push(w)
                v <- W
                v의 인접 정점 중 방문 안한 w 찾기 
        v <- pop(stack)
    } while (v)
    end
'''
